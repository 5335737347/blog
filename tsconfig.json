{
  "//": "根工程只负责检查 scripts/。测试已归属各自 workspace（apps/api/tests），不再从这里跨包引用 apps/api 源码。",
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    "noEmit": true,
    "paths": {
      "@/*": ["./apps/api/src/*"]
    }
  },
  "include": ["scripts/**/*.mts", "scripts/**/*.ts"]
}
