# 后续维护计划

Last checkpoint: 2026-09-22（全面审计与修复批次）

## 立即处理的检查项

当前 `npm run check:ci`（lint、类型检查、全部测试、文档与 OpenAPI 契约）和生产构建
已在本仓库通过；GitHub CI 也包含生产构建与浏览器 `smoke:prod`。以下事项仍应先于新的
产品功能处理：

1. 建立 SQLite、上传媒体和生产配置的自动加密异地备份，并完成一次可验证的恢复演练。
2. 浏览器关键流程测试仍有缺口：现有 `smoke:prod` 已覆盖页面渲染、导航、主题、音乐、
   目录、代码块、移动端交互，以及用户登录、留言提交、注册验证码全链路、管理员登录
   redirect 和未登录访问 `/admin` 的跳转；后台 CRUD 表单仍缺端到端断言，API 层
   用户旅程已覆盖。
3. 私有 Admin 分离尚未实施；公开 Web 目前仍同时承载管理页面，迁移前必须保持可用，
   不要继续扩大管理功能。
4. `packages/contracts` 只覆盖核心 DTO；媒体、壁纸、项目等新增端点的 Web/API 类型
   要求以 OpenAPI 和代码为准，逐步收敛，不能在文档中假定已全覆盖。

当前管理员和普通用户仍共用公开 `/api/auth/login`、`session` Cookie 和七天 JWT。
这是现状而不是目标安全边界，应在下述私有 Admin 迁移中解决，不另建一套临时认证方案。

## 注册投递与防刷准备状态

长期边界、安全规则与验收清单见
[注册验证、消息投递与防刷](registration-delivery.md)；本节只记录尚未完成的工作。

邮件服务已从 SendGrid 改为 Resend，代码已实现 HTTP API 适配器（优先）与 SMTP 回退，
并使用专用环境变量 `RESEND_API_KEY` / `RESEND_FROM`；域名 DKIM、SPF、MX、DMARC 验证
由所有者在仓库外完成。以下内容仍未完成，因此邮件注册尚不能视为完全验收：

1. 生产发送量/费用告警、退信与异常投递监控。
2. 真实投递的完整验收记录（成功、过期、错误、限流、服务商失败）。
3. Cloudflare Turnstile 的服务端校验、失败/超时/重复 Token 路径与端到端防刷测试。

手机号验证码通道已于 2026-08 整体移除，注册仅保留邮箱验证码（历史 SMS 网关、区号选择
与 E.164 规范化代码已删除）。若未来恢复，仍需遵循既有边界：主要目标为大陆 `+86`，
不采用 Twilio Verify 或免资质境外通道，先取得资质再依次完成签名、模板、凭据、
费用上限与真实号码联调。

外部服务准备完成与代码已经部署是两个独立状态，文档和发布说明必须始终明确区分。

## 当前已部署状态

- 单 Git 仓库由 npm workspaces 管理。
- `apps/web` 是监听 `127.0.0.1:3001` 的 Next.js 应用，目前仍同时包含公开页面和管理页面。
- `apps/api` 是监听 `127.0.0.1:3002` 的 Fastify 应用，负责全部数据和安全边界。
- Nginx 仅公开 Web 的 80/443；API 不直接暴露。
- `packages/contracts` 保存跨进程共享的可序列化契约。
- PM2 当前管理 `blog-web` 与 `blog-api` 两个进程。
- lint、类型检查、服务测试、Fastify 注入测试和双应用生产构建均已通过。

以上是当前事实。下述管理端方案在完成代码、部署和验收前不得写成“已经上线”。

## 已批准目标：私有管理端

目标是在同一仓库、同一服务器中增加独立 `apps/admin`，并从公开 Web 完全移除管理界面：

```text
Public Internet
      │ HTTPS :443
      ▼
    Nginx ───────────────> Web 127.0.0.1:3001

Trusted device + removable Ed25519 key
      │ restricted SSH local forward
      └──────────────────> Admin 127.0.0.1:3003

Web/Admin ───────────────> API 127.0.0.1:3002 ──> SQLite/media
```

约束：

- Nginx 不为 Admin 配置公网反向代理，防火墙不开放 3003。
- Admin 只能通过受限 SSH 本地转发访问；端口号和隐藏路径不作为安全措施。
- 使用 OpenSSH Ed25519 标准认证，不实现自定义“动态密钥”算法，也不允许网页读取或上传私钥。
- SSH 私钥使用强口令并保存在加密可移动介质；服务器只保存公钥，并准备独立恢复密钥。
- 专用 SSH 账号禁止密码、Shell、PTY、代理和任意端口转发，仅允许转发到 `127.0.0.1:3003`。
- Admin 应用仍要求管理员身份，API 对每个管理操作继续执行服务端授权。
- 手机或其他电脑只有在可信、能使用密钥并建立 SSH 隧道时才允许管理；不为移动便利开放公网 Admin。

## 实施阶段

### 阶段 1：契约与 API 边界

1. 清点现有管理页面使用的全部 API，并将管理操作统一规划到 `/api/admin/*`。
2. 将普通用户登录与管理员登录分离；公开登录端点不得签发管理员会话。
3. 管理员 Cookie、会话和撤销机制与普通用户分离，并设置更短有效期。
4. 在 Contracts、Fastify injection 测试、服务测试和 OpenAPI 中同步新边界。
5. 为公网无法取得管理员会话、普通用户无法调用管理接口增加回归测试。

### 阶段 2：创建 Admin 应用

1. 新建 `apps/admin` Next.js workspace，默认仅监听 `127.0.0.1:3003`。
2. 将 `apps/web/src/app/admin`、管理组件和管理专属客户端迁移到 Admin。
3. Admin 只通过 HTTP 和 `@kpblog/contracts` 使用 API，不导入 Prisma、API 服务或 JWT 密钥。
4. 为 Admin 增加独立类型检查、生产构建和浏览器级登录/CRUD 验证。

### 阶段 3：移除公开管理面

1. 从 `apps/web` 删除管理路由、管理组件和管理会话预检。
2. Web 不代理 `/api/admin/*`，公开站点的 `/admin` 在真实路由层面不存在。
3. 验证公开站点、普通登录注册、评论、RSS、sitemap 和 CLI 发布不受影响。

### 阶段 4：服务器访问边界

1. PM2 增加 `blog-admin`，更新脚本构建、重载、保存并健康检查三个进程。
2. 创建专用 SSH 隧道账号，登记日常公钥和独立恢复公钥。
3. 通过 sshd 约束禁止 Shell 和任意转发，只放行 `127.0.0.1:3003`。
4. 保持 Nginx 仅代理 Web，保持 UFW 仅公开 SSH、HTTP、HTTPS。
5. 在桌面和至少一种移动设备上演练密钥解锁、隧道建立、管理登录和安全退出。

### 阶段 5：切换与恢复演练

1. 切换前备份 SQLite、媒体和生产环境配置，并完成一次恢复验证。
2. 验证公开网络无法连接 3002/3003，也无法获得管理员会话。
3. 验证密钥撤销、管理员密码变更、全部会话失效和恢复密钥接管。
4. 确认新架构稳定后再删除旧兼容路径；历史由 Git 保存，不保留双后台实现。

## 验收标准

- `https://站点域名/admin` 不存在真实管理路由。
- 公网扫描只能看到 SSH、HTTP、HTTPS，3001/3002/3003 均不直接暴露。
- 没有获准 SSH 私钥时，无法建立到 Admin 的网络连接。
- 隧道密钥不能获得通用服务器 Shell，也不能转发到其他目标。
- 普通用户凭据和普通会话无法升级为 Admin。
- Admin 私钥内容、口令、恢复材料和设备路径未进入 Git、日志或应用配置；公钥指纹
  可以进入受控的运维清单，用于核对和撤销密钥。
- `npm run check && npm run build` 通过，Web、API、Admin 的关键浏览器流程通过。
- 更新、备份、恢复、密钥撤销和服务器重启后的进程恢复均有成功演练记录。

## 其他下一优先级

1. 建立 SQLite、媒体和生产配置的自动加密异地备份及保留策略。本机备份**与恢复**编排
   已接好（`npm run db:backup` / `media:backup` / `db:restore` / `media:restore`，
   见[部署手册](deployment.md#备份范围与当前恢复限制)），缺的是异地副本、加密，
   以及在隔离环境完成一次恢复演练。
2. 浏览器层测试已补上第一层：`npm run smoke`（`scripts/smoke.mjs` + `smoke-web.mjs`）
   用临时数据库与独立端口起实例，检查 11 个页面可渲染、无控制台报错与失败请求、
   内部链接可达、关键组件渲染，以及首页/文章页渲染真实数据；并断言核心交互：
   音乐面板可打开、主题可切到暗色、目录随滚动高亮、代码复制有反馈且滚动区可聚焦、
   移动端目录抽屉可开可关。已接入 `npm run check` 与 CI，并在 GitHub 上实跑通过。
   仍缺的是**表单与认证的端到端断言**（登录、注册验证码、评论提交与审核、
   管理端 CRUD、搜索下拉）；API 层的用户旅程已覆盖
   （`apps/api/tests/journey.test.ts`）。
3. 在 Web/API 分主机或多副本前，将上传媒体迁移到对象存储。
4. 前端改版已通过验收（令牌对比度 40 组全绿、axe-core 零违规、96 项响应式矩阵、
   打印样式逐条断言，见[前端设计方案](design-plan.md)第 14 节）。剩余可选项只有
   正文衬线化实验。

GitHub CI 已固定执行干净安装、Prisma Client 生成、lint、类型检查、全部测试、
文档与接口契约校验、生产构建、浏览器冒烟（对生产产物）和中危以上依赖审计。
2026-09-17 首次在 GitHub 上跑通，14 个步骤全绿。首次运行暴露的真实问题：
冒烟编排用 `npx tsx` 起 API，而 tsx 是开发依赖，在生产/CI 依赖树下 npx 会尝试
联网解析版本并失败——本地因依赖完整而看不出。现已改为运行 API 构建产物，
并把种子脚本移到 `apps/api/scripts/smoke-seed.mjs` 用 node 直接执行。

## 每次变更检查

- 先读 `docs/architecture.md`；修改 Next.js 前读当前安装版本文档。
- 接口变化同时检查 API、Web/Admin 调用方、Contracts、测试和 OpenAPI。
- 数据库变化必须包含 migration。
- 不提交 `.env*`、数据库、备份、上传媒体、构建目录或任何私钥材料。
- 提交前检查 `git status`、`git diff --cached`、`git diff`，再运行 `npm run check && npm run build`。
